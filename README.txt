TileDesk64 v1.8 FULL USER OS

Integrated:
- kernel loop
- app API
- process table
- simple IPC / event text
- shared filesystem with real paths
- shell commands:
  help, ls, cd, mkdir, rm, cp, open, cat, ps, kill
- apps:
  commander
  editor
  terminal
  htop64
  gpt
  sid
- simple disk save hook
- focus + process grid UI

Build:
  make

Run:
  x64sc tiledesk64.prg
